<Page
    x:Class="UWPTiles.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPTiles"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <NavigationView PaneDisplayMode="Top" IsBackButtonVisible="Collapsed" IsSettingsVisible="False" 
                    SelectedItem="{Binding RelativeSource={RelativeSource Mode=Self}, Path=MenuItems[0]}"
                    SelectionChanged="NavigationView_SelectionChanged">
        <NavigationView.MenuItems>
            <NavigationViewItem Content="Create tiles" Tag="TileCreationPage"/>
            <NavigationViewItem Content="Video" IsEnabled="{x:Bind TileCreationPage.IsFinished, Mode=OneWay}" Tag="VideoPage"/>
            <NavigationViewItem Content="Photo" IsEnabled="{x:Bind TileCreationPage.IsFinished, Mode=OneWay}" Tag="PhotoPage"/>
        </NavigationView.MenuItems>

        <Frame x:Name="RootFrame">
            <local:TileCreationPage x:Name="TileCreationPage"/>
        </Frame>
    </NavigationView>
</Page>
